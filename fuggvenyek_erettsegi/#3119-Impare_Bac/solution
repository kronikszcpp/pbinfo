void Impare (int& n)
{
    int S = 0, sz = 1;

    while (n)
    {
        if (n % 10 % 2 == 0)
        {
            S += (n % 10) * sz;
            sz *= 10;
        }
        else
        {
            S += ((n % 10) - 1) * sz;
            sz *= 10;
        }
        n /= 10;
    }
    n = S;
}
