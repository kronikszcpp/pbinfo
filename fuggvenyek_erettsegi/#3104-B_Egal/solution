int Egal(int n)
{
    int S;
    while (n % 2 != 1)
    {
        n /= 10;
    }
    S = n % 10;
    while (n)
    {
        if (n % 2 == 1)
        {
            if (n % 10 != S)
            {
                return 0;
            }
        }
        n /= 10;
    }
    return 1;
}
